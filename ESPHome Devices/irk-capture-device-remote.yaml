substitutions:
  # Pin package YAML and component source together for reproducible builds.
  irk_capture_ref: v1.6.1

packages:
  device:
    url: https://github.com/DerekSeaman/irk-capture
    ref: ${irk_capture_ref}
    file: ESPHome Devices/irk-capture-base.yaml
    refresh: 1d
